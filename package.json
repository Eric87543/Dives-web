{
  "name": "dives",
  "version": "1.0.0",
  "private": true,
  "description": "Dives 投資追蹤 PWA — 純前端 localStorage App；核心運算以 node:test 測試",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
